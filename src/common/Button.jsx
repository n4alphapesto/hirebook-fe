import React from 'react';

const Button = (props) => {
    return (
        <button type="button">I'm Button</button>
    );
}

export default Button;

Button.defaultProps = {

}

Button.propTypes = {

}
